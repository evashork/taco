<!DOCTYPE html>
<html>
  <body>
  	<form action="/task-register" method="post" autocomplete="on">
  		<div class="form-group">
  			<label for="date">날짜</label>
  			<input type="date" name="date" class="form-control" id="date">
  		</div>
  		<div class="form-group">
  			<label for="task">할 일</label>
  			<textarea name="task" id="task" class="form-control" cols="30" rows="3"></textarea>
  		</div>
		<button class="btn btn-primary" type="submit">Submit</button>
  	</form>
  	<h3>TODO LIST</h3>
  	<form action="/task-done" method="post">
  		<div>
  			<label>
  				<% if(todo != (null&&"")){ %>
  					<% todo.todo.forEach(function(item){ %>
						<%= item.task %> / until <%= item.date %> / finish <%= item.done %><br>
  					<%});%>
  				<% 
  				} else { %>
  					<p>등록된 일정이 없습니다.</p>
  				<% } %>
  			</label>
  		</div>
  		<button type="submit">done</button>
  	</form>
  </body>
</html>
